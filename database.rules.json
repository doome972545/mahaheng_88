{
  "rules": {
    "notify": {
      "$type": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null",
          ".validate": "$type === 'two' || $type === 'three'"
        }
      }
    }
  }
}
